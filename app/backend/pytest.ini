[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = -v --tb=short --reuse-db -x -m "not property_based"
markers =
    property_based: Hypothesis property-based tests (slow, run in CI only)